"""
Detector modules for the AgentIQ discovery runner.

Each module exposes evaluate(sf_data, sn_data, jira_data) for temporal
DetectorEvaluation capture and detect(sf_data, sn_data, jira_data) for the
existing thresholded DetectorResult opportunity output.
"""
