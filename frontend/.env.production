# Production build env for IIS deployment.
# VITE_API_BASE_URL is empty so API calls use relative URLs (/api/...).
# IIS URL Rewrite proxies /api/* to http://localhost:8000.
VITE_API_BASE_URL=
VITE_DEV_JWT=dev-token-change-me
